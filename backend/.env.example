# Copy to .env and adjust for your machine.
# PostgreSQL connection used by SQLAlchemy (driver://user:password@host:port/database)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/inventory_system

# JWT secret for authentication tokens (change in production)
JWT_SECRET=change-this-to-a-random-secret-in-production
